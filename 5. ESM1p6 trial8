14/07/26

tried adding Antarctic mask but didnt work
        sublim(i,j)=c_sublim(i,j)

        !my changes 14/07/26
        IF (j <= 24) THEN
                longwave2d(i,j) = c_longwave(i,j) + 10.0
        ELSE
                longwave2d(i,j) = c_longwave(i,j)
        END IF
        !end my changes

        sensible2d(i,j)=c_sensible(i,j)

        !my changes 14/07/26
        IF (j <= 24) THEN
                auscom_lwflx(i,j) = c_longwave(i,j) + 10.0
        ELSE
                auscom_lwflx(i,j) = c_longwave(i,j)
        END IF
        ! my changes end


15/07/26
Just trying adding a value
        longwave2d(i,j) = c_longwave(i,j) + 10.0

        auscom_lwflx(i,j) = c_longwave(i,j) + 10.0


 /g/data/lo70/sd3017/spack/1.1/release/linux-x86_64_v4/um7-git.2026.04.000_access-esm1.6-2pzzeakancmle52zl3fua5pggbu5kknt/bin/um_hg3.exe




23/07/26

######### BLOCK ONE ############
SUBROUTINE oasis3_puta2o(                                         &
#include "argd1.h"
#include "arg_atm_fields.h"
  PUT_STEP,cmessage)

  USE oasis3_atm_data_mod
  !new code added 23/07/26
  USE trignometric_mod, ONLY: true_latitude
  !done

#if defined(ACCESS)
  USE auscom_cpl_data_mod
  USE dump_sent
#endif

############ BLOCK TWO ###############
  REAL :: latentHeatOfFus=lf

  ! my changes 14/07/26
  REAL, PARAMETER :: antarctic_lat_threshold = -1.047198  ! -60 deg in radians
  ! end my changes

  INTEGER :: i              ! Loop counter


############# BLOCK THREE ############
      sublim(i,j)=c_sublim(i,j)

        !my changes 23/07/26
        if (true_latitude(i,j) < antarctic_lat_threshold) then
          longwave2d(i,j) = c_longwave(i,j) + 10.0
        else
          longwave2d(i,j) = c_longwave(i,j)
        endif
        !end my changes

        sensible2d(i,j)=c_sensible(i,j)

############# BLOCK FOUR ###############
        ! Sw flux
        auscom_swflx(i,j) = c_solar(i,j)

        !my changes 23/07/26
        if (true_latitude(i,j) < antarctic_lat_threshold) then
          auscom_lwflx(i,j) = c_longwave(i,j) + 10.0
        else
          auscom_lwflx(i,j) = c_longwave(i,j)
        endif
        ! my changes end

        ! Sensible heat flux


password for this (expires in 30 days) = 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

new link: /g/data/lo70/sd3017/spack/1.1/release/linux-x86_64_v4/um7-git.2026.04.000_access-esm1.6-2pzzeakancmle52zl3fua5pggbu5kknt/bin/um_hg3.exe

*** note: when spack installing, it installs the UM but it also starts to install the access-esm1p6 but says warning the access git file exists and wont overwrite.
so another hunch is that we also need to put the access-esm1p6 path in config. or it also needs to be overwritten

didnt work

4:00pm 
trying this without lat/lon mask, just + 50.0 wm-2 



/g/data/lo70/sd3017/spack/1.1/release/linux-x86_64_v4/um7-git.2026.04.000_access-esm1.6-2pzzeakancmle52zl3fua5pggbu5kknt/bin/um_hg3.exe 


27/07/26
payu clone -b expt -B release-piControl https://github.com/ACCESS-NRI/access-esm1.6-configs.git trial11/trial12
+ 50 wm-2

these two trials failed because of numerical instability: my source code changes of +50wm-2 were causing physical properties to blow up. 
After 2 years of drift, a variable likely exceeded realistic limits, triggering an internal model validation abort.

I found this by doing grep -iE "error|fail|stop|abort" archive/output001/atmosphere/*
where i got:
archive/output001/atmosphere/atm.fort6.pe0: Error in moisture              =    5372384151787.33       KG
archive/output001/atmosphere/atm.fort6.pe0: Error as % of change           =    5.85287507514917     
archive/output001/atmosphere/atm.fort6.pe0: ERROR IN ENERGY BUDGET       =   0.12025E+21 J/ 

My change has completely broken the atmosphere water vapor and mass conservation loops in the UM.
A moisture error of over 4 trillion kilograms (4228751206931.99 KG) means the model is wildly creating or destroying water 
out of thin air due to numerical instability. The massive energy budget error (0.62140E+20 Joules) confirms that the sheer volume 
of heat energy you added caused the underlying physics equations to diverge and crash.


28/07/26
previous experiment didnt work

trial13 (2:18pm) changes

USE trignometric_mod, ONLY: true_latitude

!my changes (28/07/26) Antarctic sea-ice-only longwave perturbation
IF (true_latitude(i,j) < 0.0 .AND. ICE_FRACTION(i,j,1) > 0.15) THEN
        longwave2d(i,j) = c_longwave(i,j) + 20.0
ELSE
        longwave2d(i,j) = c_longwave(i,j)
END IF
!end my changes

!my changes (28/07/26)
IF (true_latitude(i,j) < 0.0 .AND. ICE_FRACTION(i,j,1) > 0.15) THEN
        auscom_lwflx(i,j) = c_longwave(i,j) + 20.0
ELSE
        auscom_lwflx(i,j) = c_longwave(i,j)
END IF
!end my changes



